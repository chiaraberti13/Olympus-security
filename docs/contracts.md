# Olympus versioned contracts

This document is the wire/storage compatibility policy for every Olympus
producer, CLI, worker, database adapter, API, and report consumer.

## Canonical contracts

| Contract | Schema name | Version | Primary producers / consumers |
| --- | --- | --- | --- |
| Assessment plan | `olympus.athena.plan` | `1.0.0` | Athena plan loader, coordinator, SQLite repository |
| Scan job | `olympus.scan-job` | `1.0.0` | Athena coordinator/result storage; worker and status surfaces |
| Observation | `olympus.observation` | `1.0.0` | Scanner/sensor adapters; finding builders and future event pipelines |
| Asset | `olympus.asset` | `1.0.0` | Argus/Helios/Athena producers; Vulcan and snapshot diff consumers |
| Finding | `olympus.finding` | `1.0.0` | all assessment modules; Athena/Vulcan/report consumers |
| Evidence | `olympus.evidence` | `1.0.0` | Minerva custody and any finding/alert evidence producer |
| Event | `olympus.event` | `1.0.0` | normalized telemetry producers; Apollo rule evaluation |
| Apollo rule | `olympus.apollo-rule` | `1.0.0` | Apollo rule loader, test and streaming evaluator |
| Alert | `olympus.alert` | `1.0.0` | Apollo producer; Minerva and Vulcan consumers |
| Incident | `olympus.incident` | `1.0.0` | Minerva triage producer; report consumers |
| Security report | `olympus.security-report` | `1.0.0` | Vulcan and Athena report adapter; CLI/API/report readers |

Apollo alerts carry the source `rule_id` and validated `mitre_attack` techniques;
their optional introduction is compatible within `1.0.0`. Athena job results are
versioned as `olympus.athena.result` and contain one shared scan-job contract plus
shared assets/findings.

The authoritative JSON Schemas are generated by:

```console
olympus core export-schemas ./schemas
```

## Compatibility rules

1. Every new persisted or exchanged document carries an exact `schema_name`
   and a `MAJOR.MINOR.PATCH` `schema_version` string.
2. Consumers reject the wrong schema name, malformed versions, a different
   major version, or a newer minor version. Patch differences are compatible.
3. Models reject unknown fields. A minor release may therefore add only
   optional fields that an updated consumer knows; it must not reinterpret or
   remove existing fields. Breaking changes require a new major version and an
   explicit migration/adapter.
4. Nested contracts are validated too. A compatible collection envelope does
   not excuse an incompatible `Asset`, `Finding`, `Alert`, or `ScanJob`.
5. Consumers fail with an actionable compatibility error. They never drop an
   invalid item, invent a default interpretation, or silently coerce a future
   major version.
6. Producers write canonical current-version documents. Legacy inputs are
   accepted only through a named, tested adapter and are re-emitted in the
   current format.

## Current legacy adapters

- Athena plans created before SemVer either omitted both header fields or used
  integer `schema_version: 1`. `load_plan` maps these two known shapes to
  `olympus.athena.plan` `1.0.0`; partial/ambiguous headers remain invalid.
- Apollo maps the former rule shape only when both header fields are absent.
  Legacy events likewise require both headers to be absent and all five identity
  fields to be present; no event ID or timestamp is synthesized. Partial headers,
  catch-all rules, duplicate rule IDs, and conflicting duplicate event IDs fail.
- Vulcan still accepts the original bare arrays or single shared objects as an
  input compatibility path. Versioned Argus, Helios, Apollo, and security-report
  envelopes are validated before their arrays are consumed.
- No compatibility claim is made for unversioned Athena result documents. New
  writes and database reads validate `olympus.athena.result` and its nested job,
  asset, and finding contracts.

Migration adapters must be pure, deterministic, covered by fixtures from the
old version, and removed only after the announced compatibility window.

## Security properties

Contract validation happens before persistence or cross-module processing.
Unknown fields cannot smuggle command strings, credentials, or provider-specific
options into jobs/plans. Schemas identify structure, not authorization: callers
must still enforce engagement scope, approval, redaction, and execution policy.
